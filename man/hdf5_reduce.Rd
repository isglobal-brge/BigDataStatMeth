% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/S3_standalone.R
\name{hdf5_reduce}
\alias{hdf5_reduce}
\title{Reduce all datasets in an HDF5 group by a binary operation}
\usage{
hdf5_reduce(
  filename,
  group,
  func = "+",
  outgroup = NULL,
  outdataset = NULL,
  overwrite = FALSE,
  remove = FALSE
)
}
\arguments{
\item{filename}{Path to the HDF5 file.}

\item{group}{Group path containing the datasets to reduce.}

\item{func}{Character. Reduction operator: \code{"+"} (default)
or \code{"-"}.}

\item{outgroup}{Character or \code{NULL}. Output group
(default: same as \code{group}).}

\item{outdataset}{Character or \code{NULL}. Output dataset name
(default: same as \code{group}).}

\item{overwrite}{Logical. Overwrite existing output dataset.}

\item{remove}{Logical. Remove input datasets after reduction.}
}
\value{
An \code{\link{HDF5Matrix}} pointing to the result dataset.
}
\description{
Applies a binary reduction (\code{"+"} or \code{"-"}) across all
datasets stored in a given HDF5 group and writes the result as a new
dataset. Delegates to \code{bdReduce_hdf5_dataset()}.
}
\examples{
\donttest{
tmp <- tempfile(fileext = ".h5")
bdCreate_hdf5_matrix(tmp, matrix(1:6, 2, 3), "blocks", "A",
                     overwriteFile = FALSE)
bdCreate_hdf5_matrix(tmp, matrix(1:6, 2, 3), "blocks", "B",
                     overwriteFile = FALSE)
bdCreate_hdf5_matrix(tmp, matrix(1:6, 2, 3), "blocks", "C",
                     overwriteFile = FALSE)
result <- hdf5_reduce(tmp, group = "blocks", func = "+")
as.matrix(result)
close(result)
unlink(tmp)
}

}
\seealso{
\code{\link{hdf5_apply}}, \code{\link{cbind.HDF5Matrix}}
}
