% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/S3_split.R
\name{split_dataset}
\alias{split_dataset}
\alias{split_dataset.HDF5Matrix}
\title{Split an HDF5Matrix into multiple block datasets}
\usage{
split_dataset(x, n_blocks = NULL, block_size = NULL, bycols = FALSE, ...)

\method{split_dataset}{HDF5Matrix}(
  x,
  n_blocks = NULL,
  block_size = NULL,
  bycols = FALSE,
  out_group = "SPLIT",
  out_dataset = NULL,
  overwrite = FALSE,
  ...
)
}
\arguments{
\item{x}{An \code{HDF5Matrix}.}

\item{n_blocks}{Integer or \code{NULL}. Number of blocks.}

\item{block_size}{Integer or \code{NULL}. Rows or columns per block.}

\item{bycols}{Logical. Split by columns (\code{TRUE}) or rows (default \code{FALSE}).}

\item{\dots}{Ignored.}

\item{out_group}{Character. Output HDF5 group (default \code{"SPLIT"}).}

\item{out_dataset}{Character or NULL. Base dataset name.}

\item{overwrite}{Logical. Overwrite existing blocks (default \code{FALSE}).}
}
\value{
A named list of \code{HDF5Matrix} objects.
}
\description{
Splits an \code{\link{HDF5Matrix}} into equal-sized sub-matrices
stored as separate datasets in the same HDF5 file.

Output datasets are named \code{<out_group>/<out_dataset>.0},
\code{<out_group>/<out_dataset>.1}, \ldots (0-based index).

Exactly one of \code{n_blocks} or \code{block_size} must be provided.
}
\examples{
\donttest{
tmp  <- tempfile(fileext = ".h5")
M    <- hdf5_create_matrix(tmp, "data/M", data = matrix(1:60, 6, 10))
blks <- split_dataset(M, n_blocks = 3L)
length(blks)
lapply(blks, close)
close(M)
unlink(tmp)
}

}
\seealso{
\code{\link[BigDataStatMeth]{cbind.HDF5Matrix}}
}
