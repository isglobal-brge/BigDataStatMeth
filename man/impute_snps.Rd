% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/S3_omics.R
\name{impute_snps}
\alias{impute_snps}
\title{Impute missing SNP values in an HDF5Matrix}
\usage{
impute_snps(x, ...)
}
\arguments{
\item{x}{An \code{HDF5Matrix} containing SNP data with NAs.}

\item{...}{Ignored.}

\item{out_group}{Output group. \code{NULL} = same as input (default).}

\item{out_dataset}{Output dataset name. \code{NULL} = same as input (default, in-place).}

\item{by_cols}{Logical. Impute by columns (\code{TRUE}, default) or rows.}

\item{threads}{Integer. Number of threads (-1 = auto).}

\item{overwrite}{Logical. Overwrite existing output. Default \code{FALSE}.}
}
\value{
\code{HDF5Matrix} pointing to the imputed dataset.
}
\description{
Fills NA entries in SNP data by computing column or row means of non-missing
values. Intended for 0/1/2-coded diploid genotype matrices.
}
\examples{
\donttest{
snps <- matrix(sample(c(0,1,2,NA), 200, replace=TRUE, prob=c(.3,.3,.3,.1)),
               20, 10)
X   <- hdf5_create_matrix("snp.h5", "geno/raw", data = snps)
imp <- impute_snps(X)   # in-place; or specify out_group/out_dataset
}

}
