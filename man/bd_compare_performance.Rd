% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sysOptmization.R
\name{bd_compare_performance}
\alias{bd_compare_performance}
\title{Compare performance with and without I/O optimization}
\usage{
bd_compare_performance(
  matrix_rows = 2000,
  matrix_cols = 2000,
  operation_type = "multiplication",
  iterations = 3
)

bd_compare_performance(
  matrix_rows = 2000,
  matrix_cols = 2000,
  operation_type = "multiplication",
  iterations = 3
)
}
\arguments{
\item{matrix_rows}{Number of rows for test matrices}

\item{matrix_cols}{Number of columns for test matrices}

\item{operation_type}{Type of operation to benchmark}

\item{iterations}{Number of test iterations per configuration}
}
\value{
List with comparison results and performance metrics

List with comparison results and performance metrics
}
\description{
Runs benchmark tests to demonstrate the performance impact of
I/O-aware optimization. Uses the header-only C++ functions
for accurate performance measurement.

Runs benchmark tests to demonstrate the performance impact of
I/O-aware optimization. Useful for understanding the benefits
on your specific system and workload.
}
\examples{
# Compare performance for matrix multiplication
comparison <- bd_compare_performance(2000, 2000, "multiplication")
print(comparison)

# Compare for SVD (use smaller matrices)
comparison <- bd_compare_performance(1000, 1000, "svd")

# Compare performance for matrix multiplication
comparison <- bd_compare_performance(2000, 2000, "multiplication")
print(comparison)

# Compare for SVD (use smaller matrices)
comparison <- bd_compare_performance(1000, 1000, "svd")

}
